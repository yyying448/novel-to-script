# 剧本 YAML Schema 设计文档

## 一、设计目标与选型理由

### 1.1 为什么选 YAML

本工具定位于"AI 辅助剧本创作"，产出面向两类用户：

| 用户 | 需求 |
|---|---|
| 编剧/作者 | 可直接阅读、手动编辑剧本内容 |
| 程序/工具链 | 可解析、校验、二次加工数据 |

候选格式对比：

| 格式 | 人类可读 | 程序可解析 | LLM 生成正确率 | 结论 |
|---|---|---|---|---|
| JSON | ❌ 引号/括号风暴 | ✅ | 中等（括号嵌套偶有错误） | 不适合人工编辑 |
| Markdown | ✅ | ❌ 无固定结构 | 低（格式自由度高） | 程序解析困难 |
| YAML | ✅ 缩进即层级 | ✅ | 高（更接近自然书写） | ✅ 最佳平衡 |

**关键理由**：LLM 在生成 JSON 时容易因嵌套引号、逗号缺失而解析失败。YAML 的缩进语法与自然语言段落更接近，LLM 输出的正确率显著高于 JSON。同时 YAML 支持内嵌注释，便于编剧在剧本中标注想法。

### 1.2 设计原则

1. **机器可解析**：结构严格，字段类型明确，支持自动校验
2. **人类可阅读**：编剧能脱离工具直接看 YAML 理解剧本
3. **行业可对接**：字段设计参考电影剧本标准（Scene Heading / Dialogue / Parenthetical），便于后续导出 Fountain / Final Draft
4. **AI 增强**：增加传统剧本格式不具备的 AI 分析字段（tone / action / scene_notes / difficulty_tags）

---

## 二、Schema 层级结构

```
剧本根对象
├── scenes[]                     场景列表（必填，≥1）
│   ├── scene_id: int            全局递增场景序号（必填）
│   ├── chapter: str             所属章节标题（v2.0 新增）
│   ├── location: str            时空描述，三段式（必填）
│   ├── characters_present[]     出场人物列表
│   │   ├── name: str            角色名
│   │   └── role: str            主角 / 重要配角 / 配角 / 龙套
│   ├── summary: str             一句话场景概括（必填）
│   ├── dialogues[]              对话列表
│   │   ├── speaker: str         说话人（必填）
│   │   ├── lines: str[]         台词行数组（必填，≥1）
│   │   ├── tone: str            语气指导（AI 生成，可选）
│   │   └── action: str          同步动作（AI 生成，可选）
│   ├── scene_notes: str         编剧备注（推荐）
│   ├── estimated_duration: float 预估时长/分钟（v2.0）
│   ├── difficulty_tags: str[]   拍摄难度标签（v2.0）
│   ├── dramatic_function: str   戏剧功能（v2.0）
│   ├── conflict_types: str[]    矛盾类型（v2.0）
│   ├── conflict_intensity: int  矛盾强度 1-5（v2.0）
│   └── unfilmable_warnings: str[] 不可拍内容提示（v2.0）
└── chapter_map: dict            章节标题→原文映射（辅助字段）
```

---

## 三、核心字段设计原因

### 3.1 `scene_id` — 全局递增而非每章从 1 开始

**设计选择**：跨章节全局递增序号。

**原因**：
- 导演选角、制片排期、场务调度都需要场景的**唯一引用编号**
- 如果第 1 章有场景 1-3，第 2 章又从场景 1 开始，说"修改场景 3"会产生歧义
- 全局递增也意味着删除某场景后其余编号保持不变——编号用作**标识符**而非**排序依据**

### 3.2 `location` — 三段式格式

**格式**：`内景/外景 - 具体地点 - 时间（日/夜/黄昏/清晨）`

**设计选择**：用 ` - ` 作为三段分隔符。

**原因**：
- 参考好莱坞标准 Scene Heading（INT./EXT. + Location + Time of Day）
- 内景/外景决定灯光和收音方案，日/夜决定拍摄时段
- 用 ` - ` 分隔可程序拆分（`split(" - ")`），也符合中文书写习惯
- 不强制英文大写（INT./EXT.），因为中文剧本无此传统

### 3.3 `chapter` — 章节归属标注

**设计选择**：每个场景标注其所属章节标题。

**原因**：
- v1.0 的扁平场景列表无法回溯场景对应的原著段落
- 编剧修改时需要快速定位"第 5 章的那场戏"
- 支撑了前端按章节分组展示、按章节精准修改的功能

### 3.4 `characters_present` — 对象而非纯字符串

**设计选择**：角色用 `{name, role}` 对象，而非 `["林风", "小翠"]` 纯字符串。

**原因**：
- 纯字符串丢失了角色重要性信息
- `role`（主角/重要配角/配角/龙套）支撑了角色引擎的按重要性排序
- 群戏时能快速判断"几个主角级演员同时在场"，辅助预算评估

### 3.5 `dialogues` — 为什么拆分为 `speaker` / `lines` / `tone` / `action`

**设计选择**：每段对话包含四个独立字段。

**原因**：

| 字段 | 必要性 |
|---|---|
| `speaker` | 标准剧本必有，说话人标识 |
| `lines: str[]` | 同一角色连续说多句话放在一个对话块中，避免冗余。数组使台词分行，便于演员阅读 |
| `tone` | AI 工具的核心增值——自动推断语气（平静/愤怒/讽刺），减少导演现场解释成本 |
| `action` | 小说→剧本翻译的关键：将"他站起身走到窗前"转化为表演指令，而非保留在叙事中 |

### 3.6 `scene_notes` — 必填的编剧备注

**设计选择**：强制 LLM 为每场戏生成备注。

**原因**：
- 迫使 AI 思考"这场戏存在的戏剧意义"，而非机械翻译
- 备注内容（建立人物/推进剧情/埋下伏笔）本身就是改编质量的评估线索
- 多人协作时，备注是编剧之间沟通的桥梁

### 3.7 `meta` — 为什么需要顶层元信息

虽然当前 Schema 中 meta 为可选，但其设计意图明确：

| 字段 | 用途 |
|---|---|
| `script_version` | 支持迭代式改编（v1→v2→v3），追溯演化历史 |
| `generated_at` | 生成时间戳，与原著版本对应 |
| `adapted_from` | 记录改编章节范围，长篇分批改编时有用 |

---

## 四、v2.0 扩展字段设计原因

### 4.1 `estimated_duration` — 场景时长估算

**算法**：台词句数 × 0.1 分钟 + 转场 0.2 分钟。虽不精确但提供量级参考。

**原因**：编剧需要知道全剧总时长是否在合理范围内。制片需要初步排期参考。

### 4.2 `difficulty_tags` — 拍摄难度标签

**标签**：群戏 / 外景 / 夜戏 / VFX特效 / 动作场面 / 特技威亚 / 水下 / 情绪重场 / 代唱配乐

**原因**：制片人快速评估预算和档期。来自行业实践——剧本评估阶段就需标注技术难点。

### 4.3 `conflict_types` / `conflict_intensity` — 矛盾点标注

**类型**：人人冲突 / 人己冲突 / 人境冲突 / 权力博弈 / 情感纠葛 / 生死危机

**原因**：矛盾是剧本吸引力的核心。标注后编剧可纵览全剧冲突分布，发现"平淡段落"并加强。

### 4.4 `dramatic_function` — 戏剧功能

**取值**：建立引入 / 冲突推进 / 信息揭示 / 情感高潮 / 决策铺垫 / 转折过渡 / 收束悬念

**原因**：标准化编剧分析维度，方便从全局视角审视剧本结构完整性。

### 4.5 `unfilmable_warnings` — 不可拍内容提示

**原因**：小说的某些表达方式无法直接画面化（内心独白、抽象比喻）。标注后编剧知道哪些地方需改写，而非拍摄现场才发现"拍不出来"。

---

## 五、校验规则

| 校验项 | 严重程度 | 原因 |
|---|---|---|
| scenes 非空列表 | 致命 | 无场景的剧本不成立 |
| scene_id 存在 | 致命 | 全局编号是数据完整性基础 |
| location 存在 | 致命 | 无时空信息的场景无法拍摄 |
| location 含 ` - ` 分隔符 | 警告 | 三段式是推荐格式，且保留灵活性 |
| speaker 存在 | 致命 | 无说话人的对话无法分配 |
| lines 非空数组 | 致命 | 空对话块是数据错误 |

校验策略：致命错误阻止输出，格式警告允许继续但提示修改。

---

## 六、设计权衡与已知限制

| 权衡 | 选择 | 代价 |
|---|---|---|
| 结构化 vs 灵活性 | 倾向结构化 | 实验戏剧、诗剧等创意形式适配度低 |
| 完整性 vs Token 消耗 | 倾向完整性 | 每场戏 ≥8 个字段，长篇 Token 消耗大 |
| 通用性 vs 中文优化 | 倾向中文 | 英文剧本的 Slug Line 是 `INT./EXT.`，当前用中文"内景/外景" |
| 单层 scenes vs 幕结构 | 单层 scenes | 不支持"三幕结构"分组，幕信息由 `dramatic_function` 间接表达 |
| AI 生成 vs 人工校验 | AI 生成为主 | tone / action 等 AI 字段可能不准确，需编剧审核后使用 |
